# Visual studio code update

The script update Visual Studio Code to the lastest version. Hope you find useful :wink:

# Running the script
Make sure the script is executable 

    sudo chmod 775 update_vscode.sh

And run the shell script

    ./update_vscode.sh
